import { Typography } from "@mui/material";


const CourseInfo = () => {

    return (
    <Typography variant="h2">
        Course Info
    </Typography>
    );
}

export default CourseInfo;
