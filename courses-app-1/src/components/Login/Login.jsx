import { Typography } from "@mui/material";


const Login = () => {

    document.title = "Login";

    return (
    <Typography variant="h2">
        Login
    </Typography>
    );
}

export default Login;
